using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class InfoSystem : MonoBehaviour {
    /*
    public static Messenger GlobalFeed;
    private Messenger ChatFeed;
    public static Chat Chat;

    

    private void Start()
    {
        GlobalFeed = new Messenger(transform.Find("GlobalFeed").GetComponent<Text>(), 5);
        ChatFeed = new Messenger(transform.Find("ChatFeed").GetComponent<Text>(), 5);
        Chat = new Chat(transform.Find("ChatInput").GetComponent<InputField>(), ChatFeed, ChatFeed.Length() * 5);
    }

    private void Update()
    {
        Chat.PseudoUpdate();
    }
    */
}
