using System;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

public class Main_ButtonFunctions : MonoBehaviour {
/*
    Button B_Start;
    Button B_Quit;
    Button B_Help;
    RectTransform P_Help; 

    void Start () {
        P_Help = transform.Find("P_Help").GetComponent<RectTransform>();
        P_Help.gameObject.SetActive(false);

        B_Start = transform.Find("Buttons").transform.Find("Start").GetComponent<Button>();
        B_Help = transform.Find("Buttons").transform.Find("Help").GetComponent<Button>();
        B_Quit = transform.Find("Buttons").transform.Find("Quit").GetComponent<Button>();

        B_Start.onClick.AddListener(B_Start_Click);
        B_Help.onClick.AddListener(B_Help_Click);
        B_Quit.onClick.AddListener(B_Quit_Click);
    }
	
	public void B_Start_Click()
    {
        SceneManager.LoadSceneAsync(1);
        SceneManager.UnloadSceneAsync(0);
    }

    public void B_Help_Click()
    {
        P_Help.gameObject.SetActive(!P_Help.gameObject.activeSelf);
    }

    public void B_Quit_Click()
    {
        Application.Quit();
    }
    */
}
